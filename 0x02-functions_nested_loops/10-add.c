#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 *
 * @x: take first input
 * @y: take second input
 *
 * Return: the result of the adding x and y
 */
int add(int x, int y)
{
int result = x + y;
return (result);
}
