#include <stdio.h>
/**
 * main - Entry point.
 *
 * Description: printing numbers.
 *
 * Return: 0 Always(success)
 */
int main(void)
{
int n = 0;
while (n <= 9)
{
printf("%d", n);
n++;
}
putchar('\n');
return (0);
}
