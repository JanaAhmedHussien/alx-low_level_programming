#include "main.h"
/**
 * mul - function that multiplies two integers.
 *
 * @a: first input of the function
 * @b: second input of the function
 *
 * Return: the result of multiplying two integers together
 */

int mul(int a, int b)
{
return (a * b);
}
